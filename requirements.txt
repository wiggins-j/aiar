# AIAR core runtime.
# The harness (Ollama client + CLI + watcher GUI) needs only requests.
requests>=2.28

# --- RAG stack (optional but recommended) -----------------------------------
# Vector store + embeddings. The store degrades gracefully if these are absent
# (queries return empty and the harness answers from the bare model), so you
# can run the harness without them — but you need them for real retrieval.
#
# NOTE: on newer active Python releases, chroma-hnswlib may not have a prebuilt
# wheel for every platform/Python combination, so pip may need to build it from
# source. Install with:
#   pip install -r requirements.txt -r requirements-rag.txt
