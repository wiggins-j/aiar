# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# AIAR runtime state (vector store, grounding, logs, eval queue)
.aiar/
~/.aiar/

# User RAG corpora — documents an AI collector fetched per a Collection Brief
# (large + regenerable; the brief in briefs/ is the reproducible source of truth)
corpus/

# Local-only demo / recording scripts + private collections — never published
# (name any private brief/schema/corpus/collector files *.local.md / .json / .py)
DEMO_RECORDING.md
*.local.md
*.local.json
*.local.py

# Test / tooling
.pytest_cache/
.coverage
htmlcov/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
