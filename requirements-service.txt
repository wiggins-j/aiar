# Optional HTTP service stack for AIAR.
# Only needed to run the FastAPI harness service (aiar.harness.service:app).
# The stdlib watcher GUI (web/server.py) and the CLI need NONE of this.
#   pip install -r requirements.txt -r requirements-service.txt

fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0
